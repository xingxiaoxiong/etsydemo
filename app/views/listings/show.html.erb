

<div class="row">
  <div class="col-md-6">
    <div class="thumbnail">
      <%= image_tag @listing.image.url %>
    </div>
  </div>
  <div class="col-md-6">
    <h3><%= @listing.name %></h3>
    <p>¥<%= @listing.price %></p>
    <p><%= @listing.description %></p>
    <br />
    <div class="center">
      <%= link_to "购买", new_listing_order_path(@listing), class: "btn btn-primary" %>
    </div>
  </div>
<div>

<br />

<% if current_user == @listing.user %>
  <%= link_to '编辑', edit_listing_path(@listing), class: "btn btn-link" %> |
<% end %>
<%= link_to '返回', listings_path, class: "btn btn-link" %>